#include <iostream>
#include <string>
#include <vector>
#include <cmath>
#include <cstdlib>
#include <algorithm>  // 添加这一行

// 子函数1：计算两个数的和
void function1() {
    int a, b;
    std::cout << "请输入两个整数: ";
    std::cin >> a >> b;
    std::cout << "它们的和是: " << a + b << std::endl;
}
